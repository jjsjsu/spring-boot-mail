spring:
 mail:
  host: smtp.qq.com
  protocol: smtp
  default-encoding: UTF-8
  username: example@email.com
  password: email's authorization code
  test-connection: true
  properties:
   mail:
    smtp:
     auth: true
     starttls:
      enable: true
      required: true
server:
 port: 80
